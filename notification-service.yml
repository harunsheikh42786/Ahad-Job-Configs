server:
  port: 8084

spring:
datasource:
    url: jdbc:mysql://localhost:3306/ahad_job_portal?useSSL=false&serverTimezone=UTC
    username: root
    password: Harun@786
    spring:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

    consumer:
      group-id: job-portal-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        # ⚠️ Multiple events support ke liye default type mat do
        # spring.json.value.default.type: com.ahad.events.JobPostedEvent
